{
  "better": "<div class=\"mth\"><h3><span class=\"tag\">BETTER</span>The sketch-lock loop</h3><ol><li>Compose on the printed 21:9 frames (sheets 04&ndash;08) until silhouette, camera height and light direction are decided. Draw inside the frame &mdash; a photo of an unframed sketch silently re-centres when a model crops it.</li><li>Photograph the winner (bible &rarr; Paper &rarr; notes camera). From then on the sketch is the shot's <b>judging target</b>: every batch is selected against the drawing; a re-roll that drifts is rejected on sight. Zero rules risk &mdash; nothing external enters the project.</li><li>Prompt-only first. Escalate to sketch-as-reference only for stubborn layout (shot size, figure placement, the two match-cut pairs) after ~two failed batches &mdash; via the multi-reference image lane (nano_banana_pro) to build a photoreal comp draft. Never in the soul_2 lane: its one reference slot belongs to the lighting plate.</li><li>When a sketch goes in: &ldquo;framing, shot size and figure placement only; render photoreal; match the movement plate; no graphite, no monochrome, no paper texture.&rdquo; Sketch bleed is a hard reject. Confirm on the rules page that hand-drawn reference inputs are permitted &mdash; every input publishes with the history.</li></ol><p class=\"q\"><b>Qualifications:</b> claims precision, never credit savings &mdash; free iteration already exists on unlimited-trial models. A drawing states a composition a prompt cannot; the discipline is that the sketch stays a target by default.</p><p class=\"q\">Paper wins when a composition is easier to draw than to describe &mdash; stubborn framing, matched cuts, exact figure placement &mdash; and the sketch stays a target, not an upload.</p></div>",
  "best": "<div class=\"mth\"><h3><span class=\"tag\">BEST</span>The re-anchor budget</h3><ol><li>Classify every join at string time: <b>designed anchor</b> (drift resets), <b>inherited frame</b> (pixel-exact start, drift-from-design accrues), or <b>authored bridge</b> (near-black or blinding &mdash; drift absorbed).</li><li><b>Never more than two consecutive inherited joins.</b> A third means promote the join to a designed anchor, or spend a bridge. This film's longest runs &mdash; S6&ndash;S8 and S10&ndash;S12 &mdash; are exactly two links; the budget makes the accident a law.</li><li>Inherit <b>start frames only</b> &mdash; starts inherit pixel-exactly, end frames land approximately on every chainable model. Never chain onto an extracted end frame.</li><li>Between resets, sub-chains are independent: strict order lives inside a sub-chain, so S6&ndash;S8, S10&ndash;S12 and the standalones can generate in parallel.</li><li>At Gate D, measure each draft's landing against its designed end anchor; promote better frames in inheritance order only.</li></ol><p class=\"q\"><b>Reviewed and rejected on the way here:</b> building a long chain from both ends toward a designed midpoint &mdash; it relocates error rather than halving it, and forces earlier shots to end on extracted frames, trading exact inheritance for approximate. The budget generalises what bridge B01 already does at S11&rarr;S12.</p></div>"
}
